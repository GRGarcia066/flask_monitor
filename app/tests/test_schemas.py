from test_basic import BasicTests
import app.schemas as schemas
import app.models as models

class SchemaTests(BasicTests):

    def test_client_schema(self):
        client_schema = schemas.client_schema


schemas.clients_schema

schemas.asset_schema
schemas.assets_schema

schemas.category_schema
schemas.categories_schema

schemas.topic_schema
schemas.topics_schema
